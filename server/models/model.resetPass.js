import connection from "../config/mysql.js";

const userPasswordResetModel = async (data) => {
        console.log("password reset data from reset model.js :", data);
}

export default userPasswordResetModel;